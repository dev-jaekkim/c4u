<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.LessonMapper">

<!--개설하기-->
<insert id="insert" parameterType="java.util.Map">
	INSERT INTO lesson (lesson_id, lesson_teacher_id, lesson_name,
	        lesson_target_amount, lesson_status, lesson_start_date, 
	        lesson_fee, lesson_description,
	        lesson_category_id)
	VALUES (seq_lesson_id.NEXTVAL, #{student_id}, #{lesson_name}, 
        		#{lesson_target_amount}, 3, #{lesson_start_date}, 
      	  	#{lesson_fee}, #{lesson_description},
        		{#lesson_category_id})
</insert>

<!--재심사-->
<update id="update" parameterType="java.util.Map">
	UPDATE lesson
	SET lesson_name=#{lesson_name}
		lesson_target_amount = #{lesson_target_amount},
		lesson_status = 3
		lesson_start_date= #{lesson_start_date}, 
      		lesson_fee =#{#lesson_fee},
		lesson_description =#{lesson_description}
	WHERE lesson_id =#{lesson_id} <!-- AND lesson_teacher_id = #{student_id}-->

</update>

<!-- 심사 승인-->
<update id="updateLesson" parameterType="java.lang.Integer">
	UPDATE lesson
	SET lesson_status = 0, 
	      lesson_create_date = SYSDATE,
	      lesson_end_date= SYSDATE+30
	WHERE lesson_id = #{lesson_id} <!-- AND lesson_teacher_id = #{student_id}-->
</update>

<!-- 심사 거절-->
<!--<update id="rejectLesson" parameterType="java.lang.Integer">
	UPDATE lesson
	SET lesson_status =4,
		lesson_create_date = null,
		lesson_end_date = null
	WHERE lesson_id = #{lesson_id}--><!-- AND lesson_teacher_id = #{student_id}-->
<!--</update>-->
</mapper>


